{
  "manifest_version": "0.3",
  "name": "evals-mcp-server",
  "version": "0.1.2",
  "description": "Author verifiable eval records through a draft → review → revise → submit loop with server-enforced graders; compile to JSONL/CSV/Inspect/lm-eval via MCP. STDIO or Streamable HTTP.",
  "author": { "name": "cyanheads" },
  "repository": { "type": "git", "url": "git+https://github.com/cyanheads/evals-mcp-server.git" },
  "homepage": "https://github.com/cyanheads/evals-mcp-server",
  "server": {
    "type": "node",
    "entry_point": "dist/index.js",
    "mcp_config": {
      "command": "node",
      "args": ["${__dirname}/dist/index.js"],
      "env": {
        "MCP_TRANSPORT_TYPE": "stdio",
        "MCP_AUTH_MODE": "none",
        "NODE_ENV": "production",
        "EVALS_DATA_DIR": "${user_config.evals_data_dir}",
        "EVALS_REQUIRE_CONFIRMATION": "${user_config.evals_require_confirmation}",
        "EVALS_DEFAULT_LICENSE": "${user_config.evals_default_license}",
        "EVALS_CAPTURE_DIR": "${user_config.evals_capture_dir}"
      }
    }
  },
  "compatibility": {
    "runtimes": { "node": ">=24.0.0" }
  },
  "tools_generated": true,
  "prompts_generated": true,
  "user_config": {
    "evals_data_dir": {
      "type": "directory",
      "title": "Data directory",
      "description": "Root folder for record JSON. The store manages drafts/, submitted/, and exports/ subdirs under it.",
      "required": false,
      "default": "./evals-data"
    },
    "evals_require_confirmation": {
      "type": "string",
      "title": "Require submit confirmation",
      "description": "When 'true', evals_submit_draft requests a human confirmation where the client supports elicitation.",
      "required": false,
      "default": "false"
    },
    "evals_default_license": {
      "type": "string",
      "title": "Default record license",
      "description": "Default metadata.license applied when a draft omits one (e.g. CC-BY-4.0).",
      "required": false,
      "default": ""
    },
    "evals_capture_dir": {
      "type": "directory",
      "title": "Capture directory",
      "description": "Directory of framework-written tool-call captures; when set, captures EvalsIDs resolve to full dumps.",
      "required": false,
      "default": ""
    }
  }
}
